export default function handler(req, res) {
  if (req.method === 'GET') {
    // Fetch orders from database
    res.status(200).json([{ id: 1, status: 'In Transit' }]);
  } else {
    res.status(405).json({ message: 'Method not allowed' });
  }
}
